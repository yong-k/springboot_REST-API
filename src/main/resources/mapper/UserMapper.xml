<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.web2.mapper.UserMapper">

    <insert id="registerUser">
        INSERT INTO User (name, username, email, password, address, phone, website, company)
        VALUES (#{name}, #{username}, #{email}, #{password}, #{address}, #{phone}, #{website}, #{company})
    </insert>

    <select id="findAll" resultType="com.study.web2.vo.UserVo">
        SELECT id, name, username, email, password, address, phone, website, company
        FROM User
        ORDER BY id DESC
    </select>

    <select id="findById" resultType="com.study.web2.vo.UserVo">
        SELECT id, name, username, email, password, address, phone, website, company
        FROM User
        WHERE id = #{id}
    </select>

    <update id="updateUser">
        UPDATE User
        SET name = #{name},
            username = #{username},
            email = #{email},
            password = #{password},
            address = #{address},
            phone = #{phone},
            website = #{website},
            company = #{company}
        WHERE id = #{id}
    </update>

    <delete id="deleteUser">
        DELETE FROM User
        WHERE id = #{id}
    </delete>

</mapper>