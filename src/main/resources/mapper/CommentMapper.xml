<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.web2.mapper.CommentMapper">

    <insert id="registerComment">
        INSERT INTO Comment (postId, name, email, body)
        VALUES (#{postId}, #{name}, #{email}, #{body})
    </insert>

    <select id="findAll" resultType="com.study.web2.vo.CommentVo">
        SELECT id, postId, name, email, body
        FROM Comment
        <where>
            <if test="postId != null">
                AND postId = #{postId}
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <select id="findById" resultType="com.study.web2.vo.CommentVo">
        SELECT id, postId, name, email, body
        FROM Comment
        WHERE id = #{id}
    </select>

    <update id="updateComment">
        UPDATE Comment
        SET name = #{name},
            email = #{email},
            body = #{body}
        WHERE id = #{id}
    </update>

    <delete id="deleteComment">
        DELETE FROM Comment
        WHERE id = #{id}
    </delete>

</mapper>